spring:
  kafka:
    port: 39092
    bootstrap-servers: localhost:${spring.kafka.port}
    consumer:
      auto-offset-reset: earliest
      group-id: group-id-1
    topic: test-topic
    dlq-topic: test-dlq-topic

